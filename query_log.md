```sql
SELECT * FROM baskin_icecream WHERE Flavour='Green Tea';
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour='Green Tea';
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour='Green Tea';
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour='Green Tea';
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour='Green Tea';
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour='Green Tea';
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour='Green Tea';
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour='Green Tea';
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour='Green Tea';
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour='Green Tea';
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour='Green Tea';
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour='Green Tea';
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour='Green Tea';
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour='Green Tea';
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour='Green Tea';
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour='Green Tea';
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour='Green Tea';
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour='Green Tea';
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
INSERT INTO baskin_icecream VALUES (
            Butterscotch, 
            114, 
            7.5, 
            0.4, 
            25, 
            18, 
            2.2, 
            kids70g);
```

```sql
SELECT * FROM baskin_icecream;
```

```sql
DELETE FROM baskin_icecream WHERE id=1;
```

```sql
SELECT * FROM baskin_icecream WHERE Flavour = 'Butterscotch'
```

